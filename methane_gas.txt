#define BLYNK_TEMPLATE_ID "TMPL3gWbai4mE"
#define BLYNK_TEMPLATE_NAME "methane gas detection"
#define BLYNK_AUTH_TOKEN "FVy6PZl4v4vVbv5u7SCG2_8G9XuKfORv"

#define BLYNK_PRINT Serial
#include <ESP8266WiFi.h>
#include <BlynkSimpleEsp8266.h>
#include <DHT.h>

#define gaspin A0
#define buz D2
#define DHTPIN D1
#define DHTTYPE DHT11
DHT dht(DHTPIN, DHTTYPE);
char auth[] = BLYNK_AUTH_TOKEN;

char ssid[] = "methane";
char pass[] = "methanea5";
void setup()
{
  // Debug console
  pinMode(buz, OUTPUT);
  digitalWrite(buz, LOW);
  Serial.begin(9600);
  dht.begin();
  Blynk.begin(auth, ssid, pass);
}
       void loop ()
{
  Blynk.run();
  int ppm = analogRead(gaspin);
  Serial.print("ppm");
  Serial.println(ppm);
  int val = map(ppm, 1, 1023, 1, 1000);
  Serial.println(val);
  Blynk.virtualWrite(V1, val);
  float h = dht.readHumidity();
  float t = dht.readTemperature();
  Blynk.virtualWrite(V2, h);
  Blynk.virtualWrite(V3, t);
  if (val >= 200)
{
    Serial.println("Gas Lekaging at Your Home");
    Blynk.logEvent("notify", "Gas Lekaging at Your Home...!!");
    digitalWrite(buz, HIGH);
    delay(3000);
    digitalWrite(buz, LOW);
  }
  else
  {
    digitalWrite(buz, LOW);
  }
}
